Minimal BIDS layout for ingest + optional bids-validator.

One subject, stub 1×1×1 T1w. Not a neuroimaging study. The HTTP
`/bids/dataset_description` from Core is a separate DemoStore document;
this fixture is the ingest source for `/bids/participants`.
